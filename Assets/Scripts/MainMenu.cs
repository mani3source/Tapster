using UnityEngine;
using UnityEngine.SceneManagement;

public class MainMenu : MonoBehaviour
{
    void Start()
    { 
        SceneManager.LoadScene("MainMenu"); // Make sure "GameScene" is added in Build Settings
        s   
    }
}
